# Getting Started

Get up and running with PreShip in under 5 minutes. This guide walks you through creating an account, obtaining your API key, and running your first accessibility scan.

---

## 1. Create an Account

Sign up for a free PreShip account at [app.preship.dev/signup](https://app.preship.dev/signup). The free plan includes 50 scans per month with full accessibility scanning.

---

## 2. Get Your API Key

After signing in, navigate to **Settings > API Keys** in the dashboard. Click **Create API Key** and give it a descriptive name (e.g., "Development" or "CI Pipeline").

Copy the key immediately -- it will only be shown once. Your API key looks like this:

```
sk_live_YOUR_API_KEY_HERE
```

> **Keep your API key secret.** Do not commit it to version control. Use environment variables to store it in CI/CD pipelines.

---

## 3. Your First Scan

Run your first scan using curl:

### Request

```bash
curl -X POST https://api.preship.dev/api/v1/scans \
  -H "Content-Type: application/json" \
  -H "X-API-Key: sk_live_your_api_key_here" \
  -d '{
    "url": "https://example.com",
    "checks": ["accessibility", "performance"]
  }'
```

### Response

```json
{
  "success": true,
  "data": {
    "scanId": "scan_8f3k2j1m5n7p",
    "status": "pending",
    "url": "https://example.com",
    "checks": ["accessibility", "performance"],
    "createdAt": "2025-01-15T10:30:00.000Z"
  }
}
```

The scan runs asynchronously. Use the returned `scanId` to poll for results.

---

## 4. Get Scan Results

Poll the scan status until it completes:

```bash
curl https://api.preship.dev/api/v1/scans/scan_8f3k2j1m5n7p \
  -H "X-API-Key: sk_live_your_api_key_here"
```

### Completed Response

```json
{
  "success": true,
  "data": {
    "scanId": "scan_8f3k2j1m5n7p",
    "status": "completed",
    "url": "https://example.com",
    "completedAt": "2025-01-15T10:30:45.000Z",
    "scores": {
      "accessibility": 87,
      "performance": 92
    },
    "summary": {
      "totalIssues": 5,
      "critical": 1,
      "serious": 2,
      "moderate": 1,
      "minor": 1
    },
    "issues": [
      {
        "id": "color-contrast",
        "impact": "serious",
        "description": "Elements must meet minimum color contrast ratio thresholds",
        "help": "Ensure the contrast ratio between foreground and background colors meets WCAG 2 AA minimum thresholds",
        "helpUrl": "https://dequeuniversity.com/rules/axe/4.8/color-contrast",
        "nodes": [
          {
            "selector": ".hero-subtitle",
            "html": "<p class=\"hero-subtitle\">Welcome to our site</p>",
            "failureSummary": "Fix the following: Element has insufficient color contrast of 3.2:1 (foreground: #767676, background: #ffffff, required ratio: 4.5:1)"
          }
        ]
      }
    ]
  }
}
```

---

## 5. Understanding Results

### Scores

Each check category returns a score from 0 to 100:

| Score Range | Rating |
|-------------|--------|
| 90-100 | Excellent |
| 70-89 | Good -- minor issues to address |
| 50-69 | Needs Improvement -- several issues found |
| 0-49 | Poor -- critical issues require attention |

### Issue Severity

Issues are categorized by impact level:

| Level | Description |
|-------|-------------|
| **critical** | Blocks users from accessing content entirely |
| **serious** | Significantly impacts usability for some users |
| **moderate** | Causes difficulty but content is still accessible |
| **minor** | Minor inconvenience, low user impact |

### Next Steps

- [Set up a project](/api-reference/projects) to track scores over time
- [Install the CLI](/cli) for local development scanning
- [Add the GitHub Action](/github-action) to your CI pipeline
- [Read the full API reference](/api-reference/authentication)
